#include <QJsonValue>

class ReloadEffect {
public:
    static void exec(const QJsonValue &data);
};
class ShowMiniPlayer {
public:
    static void exec(const QJsonValue &data);
};
class HideMiniPlayer {
public:
    static void exec(const QJsonValue &data);
};
